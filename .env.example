# Local runtime
PORT=5000
DB_CLIENT=postgres
DB_EXECUTION_MODE=postgres
POSTGRES_MIGRATIONS_DIR=supabase/migrations
# Defaults to false on Vercel/serverless when omitted.
# Set to true only if you explicitly want runtime startup to do schema work.
# POSTGRES_RUN_MIGRATIONS_ON_STARTUP=false
# POSTGRES_RUN_BOOTSTRAP_ON_STARTUP=false
PG_SSL=true
PG_SSL_REJECT_UNAUTHORIZED=false
PG_POOL_LIMIT=10
BCRYPT_SALT_ROUNDS=10
CREDIT_ENTRY_DEDUP_WINDOW_MS=15000

# Supabase database and auth
SUPABASE_DB_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=profile-images
# PROFILE_IMAGE_STORAGE=local

# Local database ports
LOCAL_DEV_DB_PORT=5432
LOCAL_SMOKE_DB_PORT=55433

# Host and CORS settings
FRONTEND_ORIGIN=http://localhost:3000,http://127.0.0.1:3000,http://localhost:80,https://barman-store.vercel.app,https://barmanstore.vercel.app
CANONICAL_HOST=barmanstore.vercel.app
LEGACY_HOSTS=barman-store.vercel.app
VITE_CANONICAL_HOST=barmanstore.vercel.app
VITE_LEGACY_HOSTS=barman-store.vercel.app

# Auth flow
# Set to false only if you intentionally want backend-only local auth.
SUPABASE_AUTH_ENABLED=true
# hybrid = try Supabase first, fallback to legacy local auth
# strict = Supabase-only for email auth flows
SUPABASE_AUTH_MODE=hybrid
# Never enable unsafe JWT decode fallback in production.
SUPABASE_ACCESS_TOKEN_DECODE_FALLBACK=false
SUPABASE_EMAIL_VERIFY_REDIRECT=http://localhost:3000/login
# manual = operator-assisted delivery
# provider = auto-delivery first
AUTH_FLOW_MODE=manual

# OTP login settings
OTP_DELIVERY_MODE=manual
OTP_TTL_SECONDS=300
OTP_MAX_ATTEMPTS=5
OTP_VERIFY_SESSION_TTL_SECONDS=900
# Local manual OTP flow is easiest to test when the server exposes the generated code.
# Change this to false outside local development.
AUTH_LOGIN_OTP_EXPOSE_CODE=true

# Recommended production profile for secure OTP + OAuth auth:
# SUPABASE_AUTH_ENABLED=true
# SUPABASE_AUTH_MODE=strict
# AUTH_FLOW_MODE=provider
# OTP_DELIVERY_MODE=auto
# EMAIL_DELIVERY_MODE=auto
# AUTH_LOGIN_OTP_EXPOSE_CODE=false
# SUPABASE_ACCESS_TOKEN_DECODE_FALLBACK=false

# Email and verification settings for local development
BUSINESS_NAME=BARMAN STORE
EMAIL_DELIVERY_MODE=manual
EMAIL_VERIFICATION_MODE=stub
EMAIL_VERIFY_BASE_URL=http://localhost:3000/login
EMAIL_VERIFY_TTL_SECONDS=900
EMAIL_VERIFY_MAX_ATTEMPTS=5
PHONE_VERIFY_BASE_URL=http://localhost:3000/login
EMAIL_API_KEY=
EMAIL_API_SECRET=
EMAIL_FROM=

# Internal jobs and notifications
CRON_SECRET=
PHONE_CHANGE_CRON_SECRET=
PURCHASE_OPERATIONS_NOTIFICATIONS_ENABLED=true
PURCHASE_OPERATIONS_NOTIFICATION_INTERVAL_MS=3600000

# Frontend dev overrides
# Leave unset in normal local dev so Vite proxies /api to the backend on PORT above.
# VITE_API_BASE_URL=http://localhost:5000
# Optional explicit OAuth callback URL (useful on mobile/LAN/custom base paths)
# VITE_OAUTH_REDIRECT_URL=http://localhost:3000/login
# Preferred image provider (Google Programmable Search)
# VITE_GOOGLE_CSE_API_KEY=your_google_api_key
# VITE_GOOGLE_CSE_ID=your_custom_search_engine_id
# VITE_LOGO_DEV_TOKEN=your_logo_dev_publishable_token
# Optional fallback provider
# VITE_UNSPLASH_ACCESS_KEY=your_unsplash_access_key

# Optional server-side image provider
# SERPAPI_KEY=your_serpapi_key

# Smoke tests
SMOKE_TEST_DB_URL=
PHONE_TEST_DB_URL=
SMOKE_TEST_ALLOW_PRIMARY_DB=false
